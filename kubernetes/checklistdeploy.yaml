apiVersion: v1
kind: Service
metadata:
  name: checklist-svc
  namespace: checklistdemo
  labels: 
    name: checklist-svc
spec: 
  type: LoadBalancer
  ports:
  - name: http
    port: 3000
    targetPort: 3000
  selector:
    name: checklist-app
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata: 
  name: checklist-deploy
  namespace: checklistdemo
  labels: 
    name: checklist-app
spec: 
  replicas: 1
  template: 
    metadata:
      labels: 
        name: checklist-app
    spec:
      imagePullSecrets:
        - name: acr-secret
      containers:
      - image: subjerepo.azurecr.io/checklistsubjedocker45ca:${{ github.run_id }} 
        name: checklist-container
        resources: 
          requests:
            cpu: "20m"
            memory: "55Mi"
        env:
        - name: testEnv
          value: testEnv
        ports:
        - containerPort: 3000
          name: checklist-app
        imagePullPolicy: Always
      restartPolicy: Always